/**
 * Author: Halil Baydar
 */

// Define an enum called JobStatus
export enum JobStatus {
  // The WAITING constant is assigned the value 0
  "WAITING" = 0,
  // The RUNNING constant is assigned the value 1
  "RUNNING" = 1,
  // The FAILED constant is assigned the value 2
  "FAILED" = 2,
  // The FINISHED constant is assigned the value 3
  "FINISHED" = 3,
}
