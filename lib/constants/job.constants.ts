import { Timezone } from "../enums/timezone";

export const DEFAULT_CONCURRENCY = 1;
export const DEFAULT_LOCK_LIMIT = 1;
export const DEFAULT_TIMEZONE = Timezone.EUROPE_ISTANBUL;
export const DEFAULT_JOB_TYPE = "x";
export const DEFAULT_PRIORITY = 0;
export const DEFAULT_LOCK_EXPIRE = 60 * 1000; // one minute
